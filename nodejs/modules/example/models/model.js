const { Model } = require('../../../index')

/**
 * Each module's model should be concerned with managing
 * data for the given module.
 *
 * Ideally this class should be used to abstract verbose
 * operations and data management logic.
 *
 */
class ExampleModel extends Model {

}

module.exports = ExampleModel
